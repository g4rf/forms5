# forms5

HTML5-Formulare für Fahrtkostenerstattungen, Auslagenerstattungen und 
Aufwandsentschädigungen.

## Installation

- Umbennen der `js/config.sample.js` in `js/config.js`
- Anpassen der Daten in der `js/config.js`